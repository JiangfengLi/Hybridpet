# 基因表（设计稿）

来源：用户 2026-09-23 10:01 的原文。**本文件只做「规范化 + 编号修正 + 补全计数」，
不改动任何一个选项的文字**。凡是本文档新增的编号、计数、结论，都在标题或备注里标明是我加的。

同伴信息：
- 现有生物的实现位置 `index.html:2201`（`SPECIES_LIST`）／`index.html:1829`（`spawnAnimal`）
- 资产实测参数：`node _probe_params.mjs`（运行时）、`E:/anaconda/python.exe _probe_glb.py models`（GLB 原始值）

---

## 0. 原文的编号问题（先说清，否则后面全对不上）

1. 原文第一个块用了序号 1~5，第二个块开头写「下方序号都后移二位」，
   但第二块实际又从 **4** 开始写（4 眼·有无 / 5 眼·颜色 …）。
   ⇒ 全表存在**两组序号重叠**（4、5 各出现两次，一个是"主体质感/主体形态"、一个是"眼"）。
   本文档统一重编为 **1~32**（重编是我的动作，原序号列在"原编号"里备查）。
2. **角冠·形态缺 E**：原文写 `A羊角 B牛角 C独角兽 D王冠 F头盔 G机器天线` —— 从 D 直接跳到 F。
   本文档按原样保留 6 项并标出缺口，**没有替你补 E**（补什么由你定）。
3. **9 行漏了序号**：翼·形态 / 尾·有无 / 尾·颜色 / 尾·形态 / 背脊特殊有无 /
   背脊特殊·颜色 / 背脊特殊·形态 / 四肢·颜色 / 四肢·形态 …… 原文里这些行直接跟着上一行列出来，
   没有独立序号。本文档按出现顺序补编。
4. **「见下一项」= 计数就是下一行列出的选项个数**。本文档把它换成实际数字，方便算组合空间。

---

## 1. 规范化的基因表（32 项）

| 序号 | 原编号 | 基因组 | 取值数 | 选项（照抄原文，未改动） |
|---|---|---|---|---|
| 1 | 1 | 骨骼 | 1 | A：demo 全使用无骨骼质点来确定生物及其运动。 |
| 2 | 2 | 体型（为游戏内实际） | 6 | A：同 slime-ring；B：比 slime-ring 大 40%；C：比 slime-ring 大 80%；D：比 slime-ring 大 120%（三维都大）；E：长宽大 80%，高相同；F：高大 80%，长、深相同。 |
| 3 | 3 | 主体色 | 13 | A~E 红黄蓝绿紫；F~J 浅红黄蓝绿紫；K 灰；L 黑；M 白 |
| 4 | 4 | 主体质感 | 8 | A 光滑 / B 黏滑 / C 粗糙 / D 胶质 / E 结晶 / F 金属 / G 毛绒 / H 甲壳 |
| 5 | 5 | 主体形态 | 4 | A 球形；B 奶龙；C 标准哺乳动物；D 史莱姆形（现有的两个史莱姆算球形） |
| 6 | 4(2) | 眼·有无 | 2 | 有 / 无 |
| 7 | 5(2) | 眼·颜色 | 13 | A~E 红黄蓝绿紫；F~J 浅红黄蓝绿紫；K 灰；L 黑；M 白 |
| 8 | 6 | 眼·形态 | 4 | 动物眼 / 机器眼 / 蜗牛眼 / emoji 眼 |
| 9 | 7 | 耳·有无 | 2 | 有 / 无 |
| 10 | 8 | 耳·颜色 | 13 | 同上 13 色 |
| 11 | 9 | 耳·形态 | 4 | 动物耳 / 机器耳 / emoji 耳 / 精灵耳 |
| 12 | 10 | 角冠·有无 | 2 | 有 / 无 |
| 13 | 11 | 角冠·颜色 | 13 | 同上 13 色 |
| 14 | 12 | 角冠·形态 | 6 | A 羊角 / B 牛角 / C 独角兽 / D 王冠 / **（原文缺 E）** / F 头盔 / G 机器天线 |
| 15 | 13 | 翼·有无 | 2 | 有 / 无 |
| 16 | 14 | 翼·颜色 | 13 | 同上 13 色 |
| 17 | （无序号） | 翼·形态 | 3 | A 羽翼 / B 翼龙的翼 / C 螺旋桨 |
| 18 | （无序号） | 尾·有无 | 2 | 有 / 无 |
| 19 | （无序号） | 尾·颜色 | 13 | 同上 13 色 |
| 20 | （无序号） | 尾·形态 | 4 | A 兽尾 / B 恐龙尾 / C 鱼尾 / D 机器尾巴 |
| 21 | （无序号） | 背部特殊·有无 | 2 | 有 / 无 |
| 22 | （无序号） | 背脊特殊·颜色 | 13 | 同上 13 色 |
| 23 | （无序号） | 背脊特殊·形态 | 4 | A 龙鳍 / B 剑骨 / C 背着机枪 / D 背着背包 |
| 24 | （无序号） | 四肢·有无 | 2 | 有 / 无 |
| 25 | （无序号） | 四肢·颜色 | 13 | 同上 13 色 |
| 26 | （无序号） | 四肢·形态 | 2 | A 标准小动物 / B 标准大动物 |
| 27 | （无序号） | 爪·有无 | 2 | 有 / 无 |
| 28 | （无序号） | 爪·颜色 | 13 | 同上 13 色 |
| 29 | （无序号） | 爪·形态 | 2 | A 尖锐 / B 巨大 |
| 30 | （无序号） | 发光器官·有无 | 2 | 有 / 无 |
| 31 | （无序号） | 发光器官·颜色 | 13 | 同上 13 色 |
| 32 | （无序号） | 发光器官·形态 | 2 | A 安康鱼 / B 机器指示灯 |
| **33** | （新） | **附属物数量** | **4** | A 0 / B 1 / C 2 / D 3 |
| **34** | （新） | **附属物1** | **5** | A 剑 / B 盾牌 / C 天使光环 / D 卫星式光环 / E 炸药包 |
| **35** | （新） | **附属物2** | **5** | 同上（用户给的三行字面完全相同） |
| **36** | （新） | **附属物3** | **5** | 同上 |

> 33~36 是用户 2026-09-23 12:34 追加的。**结构与前面 9 组"部件"不同**：
> 前面是「有无(2) + 颜色(13) + 形态(k)」，这四行是「**数量(4) + 3 个形态位(各 5)**」，**没有颜色位**。
> 照原样实现，没有替用户补颜色（"附属物要不要也有颜色"见 §17）。

**合计 32 个基因位。**

---

## 2. 组合空间

| 算法 | 结果（36 位） | 说明 |
|---|---|---|
| 直接相乘（忽略"有无/数量"） | ≈ **2.5 × 10²³** | 骨骼1 ×体型6 ×主体色13 ×质感8 ×形态4 × 9 个部件类(2×13×k) × 附属物(4×5×5×5) |
| 按"不表达"折叠 | ≈ **1.9 × 10²⁰** | 部件类写成 `1(无) + 1×13×k(有)`；附属物写成 `1+5+25+125 = 156`（数量 0/1/2/3） |

> 加附属物之前是 5×10²⁰ / 1.2×10¹⁸；这 4 位把空间放大了 500 倍（折叠后 156 倍）。

两个数都远超"能看出重复"的量级。**这不是说表设计错了**，但它有一个直接后果：
如果每个个体在 32 个位上**独立随机**，你会得到一堆"互不相干的一次性拼贴"，
而"繁衍"这件事在观感上就**没有遗传** —— 子代和亲代不会像。
（这条我列在下面的建议里，也给了一个不改你任何选项的做法。）

9 个部件类的颜色维度是**各自独立的 13 档**（`眼/耳/角冠/翼/尾/背脊/四肢/爪/发光`），
光"颜色"就占了 9×13 = 117 个基因位。

---

## 3. 现有两种史莱姆的归类

⚠️ 先说结论：**能干净归类的只有 5 项**（骨骼 / 主体形态 / 主体质感 + 两者的颜色近似），
**体型只能归一半**，**9 个部件类全部是"无"**，另有 1 项**结构性缺口**（见 3.4）。

### 3.1 能归的

| 基因组 | slime-ring | slime-ball | 依据（可核） |
|---|---|---|---|
| 1 骨骼 | **A**（无骨骼质点） | **A** | GLB 无 skin/joint、动画 0；运动由 `sampleHop()` 程序化驱动（`index.html:2383`） |
| 4 主体质感 | **D 胶质**（最贴合） | **D 胶质** | 运行时 `opacity 0.68 + roughness 0.22 + metalness 0`；原始 GLB 是 `KHR_materials_transmission 0.5 / ior 1.35` 的果冻 |
| 5 主体形态 | **A 球形** | **A 球形** | 你原文已注明"现有的两个史莱姆算球形" |

### 3.2 颜色：只能算"近似落档"

| 基因组 | slime-ring | slime-ball |
|---|---|---|
| 3 主体色 | 落在 **M 白 ↔ K 灰 之间**（贴图均色 `#d2ccbd`） | 落在 **H 浅蓝**（贴图均色 `#79b4d8`） |

⚠️ 但要如实说：**这两个模型的颜色是"贴图渐变"，而你的颜色基因是"13 个单色档"** ——
严格讲它们**落不进任何一档**。均色是我把贴图缩到 32×32 求的平均，
slime-ring 的贴图里同时有 `#c0c0c0`（51%）与 `#c0c0a0`（32%）两个主色，说明它本身是花的。

### 3.3 体型：slime-ball **落不进任何一档**

以 slime-ring 为基准（实测裸几何，s=1.00：宽 1.488 / 高 1.832 / 深 1.869 m）：

| | 宽 | 高 | 深 |
|---|---|---|---|
| slime-ball 实测 | 2.537 | 1.832 | 2.120 |
| 相对 slime-ring | **+70.5%** | **+0.0%** | **+13.4%** |

- 最接近的是 **E（长宽大 80%、高相同）**，但宽只大了 70.5%、深只大了 13.4%。
- 而且 **E 要求"长宽"一起放大（等比）**，而实测是 宽 +70.5% / 深 +13.4% —— **连等比都不是**。
  ⇒ 它**不是** slime-ring 的缩放版，是**另一个 GLB 模型**（三角 4,959 vs 4,840，贴图不同，比例不同）。

### 3.4 结构性缺口（归类时最大的一个问题）

> **⚠️ 本节已被用户 2026-09-23 10:16 的澄清推翻，保留原文备查。**
> 用户原话：「**导入的 GLB 模型和主体形态没有直接关系**。你的碰撞体积只和导入模型大小有关（圆柱）。」
> ⇒ 模型由 Tripo3D 生成、自带 AI 的理解，**本来就不会和基因完全一样**，这是设计卖点。
> ⇒ 所以"主体形态"只是**提示词里的一个词**，不是"必须在几何上落实"的维度；
>   两个史莱姆不需要被归进"主体形态" —— 也就**不存在"表里缺一维"这个问题**。
> ⇒ 附带的后果（重要，见 §8.4）：主体形态因此**不可断言**；
>   而碰撞体只能**在模型生成之后量**，不能由基因直接指定。

表里有「主体形态」这一维（球形 / 奶龙 / 标准哺乳动物 / 史莱姆形），
但**没有"同一个形态下用哪个网格"这一维**。
而现实是：**"A 球形"底下已经有两个互不相同的实现**（两个不同的 GLB）。

⇒ 这意味着一件事：**"导入的 GLB 模型"本身其实是一个独立的维度**。
如果要让表能描述现在的世界，至少要在"主体形态"下再挂一层"变体/编号"，
或者干脆把「模型」列成一个顶层基因位。

### 3.5 9 个部件类：全部「无」

| 基因组 | slime-ring | slime-ball | 依据 |
|---|---|---|---|
| 6~8 眼 | **无** | **无** | 单节点 / 单网格 / 单图元，无独立零件 |
| 9~11 耳 | 无 | 无 | 同上 |
| 12~14 角冠 | 无 | 无 | 同上 |
| 15~17 翼 | 无 | 无 | 同上 |
| 18~20 尾 | 无 | 无 | 同上 |
| 21~23 背脊 | 无 | 无 | 同上 |
| 24~26 四肢 | 无 | 无 | 同上 |
| 27~29 爪 | 无 | 无 | 同上 |
| 30~32 发光器官 | 无 | 无 | 材质无 `emissiveMap`、`emissive` 未设 |

⚠️ **一条我无法验证的事，写在这里免得被当成"已确认"**：
以上都是**几何/材质层面**的结论（没有独立零件、没有 emissive）。
但**贴图上是否"画"了眼睛或花纹**，我从 GLB 数据里判断不出来 —— 那需要看图。
如果要严格归类"眼·有无"，得先确认这两个模型的基色贴图上有没有画眼睛。
（要的话我可以把两张基色贴图导出成 PNG，你一眼就能判断。）

### 3.6 归类结果一句话版

两个史莱姆 = `骨骼A + 主体形态A球形 + 主体质感D胶质 + 部件类全"无"`，
颜色近似 `slime-ring≈白/灰、slime-ball≈浅蓝`，
体型：`slime-ring = A（按定义）`、`slime-ball = 表里没有对应档`。

---

## 4. 我判断不了的（需要你给判据，我不猜）

1. **主体形态 B 奶龙 vs D 史莱姆形**：这两档在几何上怎么区分？（都是"团块"的话我没法归）
2. **主体质感 B 黏滑 vs D 胶质**：现有素材同时满足"半透明 + 光滑"，这两档的界线在哪？
3. **主体质感 C 粗糙 vs H 甲壳**：同上。
4. **角冠·形态 原本的 E 是什么？**
5. **体型「同 slime-ring」以哪一只为准？** slime-ring 的实例大小是**产生时随机**取的
   （`s ∈ [0.70, 1.00]`，实测场上 0.70~0.99）——
   所以"同 slime-ring"这个基准本身有 ±30% 的浮动。需要一个固定基准（建议钉在 `s = 1.00` 的裸几何）。
6. **「有无=无」时，颜色/形态两个基因位算不算存在？**（影响繁衍时的遗传分配 —— 见 5.3）

---

## 5. 我的建议（与你的设计分开，供取舍）

> 以下全部是我加的。**没有改动你任何一个选项**。

### 5.1 先定一个"绝对尺寸基准表"，把体型 6 档落成米（可直接抄）

以 slime-ring 的裸几何（s=1.00）为基准 —— 宽 1.488 / 高 1.832 / 深 1.869 m：

| 档 | 宽 | 高 | 深 | 场景参照（现有世界） |
|---|---|---|---|---|
| A 同 slime-ring | 1.49 | 1.83 | 1.87 | 比草（1.2 m）高、比路灯（4 m）矮一半 |
| B 大 40% | 2.08 | 2.56 | 2.62 | 和人差不多高 |
| C 大 80% | 2.68 | 3.30 | 3.36 | 接近路灯（4 m） |
| D 大 120% | 3.27 | 4.03 | 4.11 | **和路灯一样高**，比树（9.4 m）矮但已经很显眼 |
| E 长宽+80% 高不变 | 2.68 | 1.83 | 3.36 | 一张"飞碟"，宽 3.4 m 比营地门还宽 |
| F 高+80% 长深不变 | 1.49 | 3.30 | 1.87 | 一根"瘦高柱子"，宽只有 1.5 m |

建议给每档配一个"在世界里等于谁"的锚点（上表最后一列），
否则 C/D 在代码里只是两个数字，到场上才发现"原来这么大"。

### 5.2 体型基因与现有的 `s` 功能重叠 —— 必须二选一

现在每只个体产生时会在 `s ∈ [0.70, 1.00]` 里**随机**取一个缩放
（`index.html:2214-2215` 的 `s` 字段；实测场上 slime-ring 0.70~0.99、slime-ball 0.72~1.00）。

如果新的"体型"基因也管大小，两边会乘在一起：
**实际大小 = 体型档位 × s**，于是"体型 C 的个体"实际会在 2.68~3.82 m 之间漂。
⇒ 需要决定：体型基因**取代** `s`（个体差异改由别的机制提供），还是与 `s` 叠加（那就要接受这个浮动）。
（我倾向取代 —— 但这属于你的设计决定，我不动。）

### 5.3 「有/无」的语义要想清楚，否则繁衍会出现"隐性基因" bug

比如 `眼·有无 = 无` 时，`眼·颜色` 与 `眼·形态` 还有值吗？

- **占位保留**：无眼但记着"红/蜗牛眼"，繁衍时可能隔代重现 —— 像真实的隐性基因，好玩但难解释。
- **清空**：简单、可解释，但"无眼个体"的后代永远是随机颜色。

两条都合理，**但必须明确选一条并写进规则**，否则实现时会无意中选到其中一条，
而玩家看到的"为什么这只突然有眼睛了"就没法解释。这一条**建议在实现前就定**。

### 5.4 颜色维度重复 9 次：独立 vs 共享色板

`眼/耳/角冠/翼/尾/背脊/四肢/爪/发光` 各有独立的 13 色 —— 9×13 = 117 个基因位。
- 独立：能做"红眼睛 + 蓝尾巴"，表现力强，但**配色容易脏**（32 个位里 9 个能出撞色）。
- 共享色板（比如按"主体色"派生，只允许调明度/偏离）：好调、一眼像"一个物种"，但少个性。

**折中做法（不改你的选项）**：保留 9 个独立颜色位，但繁衍/生成时加一条**配色约束**
（例如"离主体色不超过 N 档"）——这样表还是你的表，只是取值不再完全自由。

### 5.5 遗传感：让子代"像亲代"，否则繁衍没有意义

组合空间 ≈ 10¹⁸~10²⁰ ⇒ 独立随机的话，**任何两只都不会像**，
"繁衍"就退化成了"再随机一只"。建议（只是建议，规则你定）：

- **分层**：先定一个"族/物种"层（少量固定组合），个体只在该族内部小幅变异。
- **继承**：子代每个基因位以概率 p 取父方、1−p 取母方（p 由你定），再以低概率变异；
  **变异幅度受档位相邻性约束**（颜色只能跳到相邻档，不能从红直接跳黑）。
- 这样"看三代能看出亲缘"这件事才成立。

### 5.6 编号与漏项（纯编辑问题，建议在实现前修掉）

1. 序号重叠（两个 4、两个 5）→ 按本文档的 1~32 重编。
2. 角冠·形态 的 **E** 缺失。
3. 9 行缺序号。
4. 「见下一项」建议统一换成实际数字（本文档已换），后面写代码时要用。

---

## 6. 落地约束（技术，来自现有代码 —— 这几条会直接决定实现方式）

1. **同物种的 20 只共用同一个 `geometry` 与 `material` 实例**（实测：`geo=true mat=true`）。
   ⇒ 要让"主体色"按个体生效，**必须先 clone 材质**（现在屠宰的灰度化就是这么做的）。
   否则改一只 = 改整个物种。
2. **碰撞圆柱体是"产生时"算一次的**（`measureCylinder()` `index.html:1799`，在 `spawnAnimal()` 里调用）。
   ⇒ 体型基因**必须在生成实体之前定好**；如果允许体型在生命周期里变化，圆柱要跟着重量。
3. **颜色目前 100% 来自贴图**，材质 `color` 是纯白 —— 不存在"物种色值"这种参数。
   ⇒ 单色档的"主体色"最省的做法是改 `material.color`；要更细就得动贴图。
4. **GLB 里的法线贴图与金属粗糙度贴图当前被丢弃**（`normalizeModelMaterial()` `index.html:1474`
   重建材质时只 copy 了 `map`）。
   ⇒ 如果"主体质感"里的 金属/结晶/胶质 打算靠这三张贴图表现，得先改这个函数把它们接回来。
   （现在 8 档质感只能靠 `roughness / metalness / opacity / color` 四个数值表达。）
5. **无骨骼**（骨骼基因只有 A）⇒ 所有附件（耳/翼/尾/四肢…）都只能是挂在 Group 上的子节点，
   不能做骨骼动画；它们的形变要靠 Group 的 transform 或顶点级形变（现有的弹跳就是顶点以外的方式：
   直接缩放 `ud.body.scale`）。

---

## 7. 待办（本文件只是设计记录，尚未实现任何基因逻辑）

- [ ] 用户确认 §4 的 6 个判据问题
- [ ] 用户确认 §5.2（体型 vs `s`）与 §5.3（有无的语义）
- [ ] 修 §5.6 的编号与漏项
- [ ] 之后再谈实现（繁衍页目前仍是三格骨架，未动）

---

# 第二部分：繁衍与基因的规则（用户 2026-09-23 10:16 给出）

> 这一部分是**用户的规则**，照抄不改；我的问题与建议全部放在 §9，分开写。

## 8. 规则原文（用户口述）

### 8.1 slime-ball 的体型

> 「slime-ball 调整成 E 大小。」

### 8.2 基因的用途与"不精确"是卖点

> 「基因用于繁衍系统，之后连接 tripo3D 生成模型（现在还没有 api，先生成一个球代替），
>  本来就有 ai 自己的理解，不会和基因完全一样，这是我们的卖点：每个繁衍后代都独一无二。
>  因此你要理解，**导入的 GLB 模型和主体形态没有直接关系**。
>  你的**碰撞体积只和导入模型大小有关（圆柱）**。」

### 8.3 繁衍流程

> 「在两个槽位中分别填入两个背包中的帕鲁，之后点击交配。
>  点击后开始生成模型，UI 的繁衍界面的这两个槽位会有一个 **90 秒倒计时**，
>  倒计时结束时**提醒玩家接生**，接生时播**出动画**，结束后得到后代，
>  之后后代帕鲁**回到背包**，亲代帕鲁**留在槽位**，方便用户继续繁衍或者点击帕鲁放回背包。
>  **动画你不需要做。**」

### 8.4 基因逻辑

> 「亲本的基因**各有 50%** 进入子代基因，每个基因位置有 **5% 突变率**
>  （发生突变时告诉玩家**总共突变了哪些性状**，不具体说是什么性状）。
>  后代基因在**点击交配之后立即计算并确定**，但**不告诉用户**。
>  后代基因用于生成**提示词**，给 tripo3D api 生成模型。」

### 8.5 由 8.2 推导出的两条实现含义（我的理解，供你确认）

1. **"主体形态"基因不需要在几何上落实** —— 它只是提示词的一部分，
   实际形状由 Tripo 的 AI 决定。⇒ 归类问题（原 §3.4）消失。
2. **碰撞圆柱不能在生成前算** —— 必须**等模型到手后量**
   （现有 `measureCylinder()` 正好就是"产生时量一次"，方向是对的）。
   ⇒ 但"体型"基因怎么影响碰撞体，必须先定归一化规则（见 §9.3）。

---

## 9. 评审：实现前必须定的问题

按"不定就没法动手"排序。**每条我都给了具体数字或代码位置，不是泛泛而谈。**

### 9.1 ❗ 90 秒倒计时用哪个时钟 —— 用错会直接坏掉

现状（可核）：`index.html:3719`
```js
const simRunning = !paused && !bag.open && !debugStill;
if (simRunning) tAcc += dt;
```
⇒ **开着背包面板时模拟是冻结的**（`tAcc` 不推进），而繁衍页就在背包面板里。
所以：

| 选法 | 后果 |
|---|---|
| 用**模拟时间**（`tAcc`） | 玩家**必须**开着繁衍页干等 90 秒；一旦关面板/去逛世界，倒计时就停了。体验是"罚站 90 秒" |
| 用**真实时间**（`Date.now()`） | 玩家可以关面板去玩，回来接生。**但要额外处理"暂停(Esc)"**：暂停时倒计时还走吗？ |

**必须选一条。** 我建议真实时间（等待期间玩家有事可做），并明确"暂停时是否也走表"。

### 9.2 ❗ 跨页拖拽做不到 —— 这是最可能卡住实现的一处

现状（可核）：`index.html:3553` `setBagPage()` 只切 class，两个页签**互斥显示**；
而且 `renderBag()` 是整块 `innerHTML` 重渲染（拖放靠 `data-zone` 事件委托活的）。
⇒ **玩家没法同时看到"帕鲁页的背包"和"繁衍页的槽位"**，
所以"从背包拖进槽位"这条操作**在现有 UI 结构下不成立**。

三条可选（都能做，代价不同）：
1. **繁衍页里也放一个迷你背包区**（只读列表 + 可拖）—— 改动集中在繁衍页，帕鲁页不动。
2. **改成交互不是拖拽**：在帕鲁页点选一只 → 再点繁衍页的槽位（现有的"点选"模式已经存在于 `selected` 变量里）。
3. **做跨页拖拽**：切页时源元素被销毁，需要在 `dragstart` 时把数据拷出来、切页后再命中 —— 最麻烦。

**需要你选。** 需求 3.1 只写了"填入两个背包中的帕鲁"，没写方式。

### 9.3 ❗ 体型基因会被 Tripo 返回的模型吃掉（技术方案最大的漏洞）

现有落地方式（可核）：`loadModel()` `index.html:1374` 做的是
"烘节点变换 → **底面归零** → 返回几何"，然后由 `spawnAnimal()` 乘**一个标量** `baseScale`。

问题：**Tripo 返回的模型有没有确定的尺度？** 如果每次生成的模型大小是随机的，
那么"体型 E 的后代"和"体型 A 的后代"在场上会**长得一样大** ⇒ 体型基因变成摆设。

⇒ 必须规定落地规则，建议：
```
归一化：把生成回来的模型按其包围盒等比缩放到"参考高度 1.832 m"，再乘体型档位系数
体型系数：A=1.00（等高）… 用 §5.1 那张表的米数反推（等比档直接是倍数）
```
**这条不定，体型基因就是装饰品。** 请确认。

### 9.4 ❗ 模型就绪 ≠ 倒计时结束

Tripo3D 生成一个模型要几十秒到几分钟（不确定）。而规则写的是"90 秒倒计时结束 → 提醒接生"。

三种情况必须定：
- 90 秒到了但**模型还没回来** ⇒ 接生拿什么显示？（等下去？先给个占位球？）
- 模型**永远不回来**（超时/失败/额度） ⇒ 重试？退默认球？还是干脆不进背包？
- 模型**很早就回来**（比如 20 秒）⇒ 倒计时是继续走满 90 秒，还是提前结束？

建议："**倒计时 = 最短等待时间，模型就绪 = 可以接生**"，两者都满足才允许接生；
超时（比如 5 分钟）走一个兜底（给一颗程序化球并按基因染色）。**请确认。**

### 9.5 ❗ "突变时告诉玩家哪些性状"与"不告诉用户"在时机上冲突

规则说：基因在**点击交配时**立即确定但**不告诉用户**；又说突变时要告诉玩家"哪些性状"。
⇒ 如果突变提示在**交配时**出现，就等于泄题（玩家立刻知道哪个位变了）。
⇒ 建议把提示放在**接生时**（和后代的出生结果一起给）。**请确认时机。**

同时"哪些性状"这个措辞有歧义，两种读法：
- (a) 告诉**哪些基因位**发生了突变（如"眼·颜色 发生了突变"），但不说新值；
- (b) 只说**突变了几个位**，不说哪些。
我按 (a) 理解（因为"哪些性状"指的就是位点）。**请确认。**

### 9.6 ❗ 突变"跳到哪一档"没有定义

5% 只说"发生突变"。但 **13 档颜色突变时跳到哪一档**没说。两种做法观感差很远：

| 做法 | 红(主体色A) 突变后可能变成 | 观感 |
|---|---|---|
| 整表随机 | 浅红 / 黄 / 蓝 / 灰 / 黑 … 任意 | 剧烈，"变异感"强，但可能变得完全不像亲代 |
| 只跳相邻档 | 浅红（F）或黄（B） | 温和，像"渐变"，但不容易看出"突变" |
| ±N 档 | 由你定 N | 可调 |

**请选一条。** 这条直接决定"突变"在画面上看不看得出来。

### 9.7 5% × 32 位的真实分布（我用二项分布算的，供你判断手感）

每位**独立** 5% ⇒ 期望突变 1.60 位：

| 恰好突变的位数 | 概率 |
|---|---|
| **0 位（完全不突变）** | **19.37%** |
| 1 位 | 32.63% |
| 2 位 | 26.62% |
| 3 位 | 14.01% |
| ≥5 位 | 2.04% |

⇒ **约每 5 只后代就有 1 只完全不突变**（和亲代一模一样）。
如果卖点是"独一无二"，你可能希望这个数更小 —— 那就得提高突变率（比如 10% ⇒ 0 位只剩 3.4%）。
**这可能是你要的，但你要知道这个数。**

另外："每位 5%"和"整体 5% 概率发生一次突变"是两回事，我按**前者**理解。**请确认。**

### 9.8 数据一致性：亲代"留在槽位"必须真的从背包移出

现在 `pals` 是背包数组（`index.html:2630`）。若槽位里的亲代**仍留在 `pals`**，
同一只帕鲁就会同时存在于两个容器 ⇒ 立刻出现一批不一致：
- 背包计数显示错（栏位里显示"已上场"还是照常占用格？）
- **屠宰块**：玩家能把槽位里的亲代拖去屠宰吗？（现有屠宰只认 `pals`）
- `&fill` / `&slay` 调试参数与现有 44 条拖放断言会不会被影响

⇒ 建议：拖入槽位 = **真的从 `pals` 摘出**、放进 `slots[]`（和 `mag → pals` 的搬法同一套语义）。
**请确认。**

### 9.9 同一只帕鲁不能同时占两个槽位

规则没说，但"自交"的逻辑是未定义的。需要一条硬约束：
**已在槽位 A 的帕鲁，不能再拖进槽位 B**（UI 上要能看出"这只已在场上"）。
另外：**两个槽位有性别吗？** 规则里没有性别字段 ⇒ 我按"无性别、任两只都可配对"理解。
**请确认。**

### 9.10 刷新即丢失（现在没有存档）

现在整个游戏**没有持久化**。⇒ 繁衍成果刷新即清空。
如果这是可接受的（demo 阶段），那没问题；如果你希望后代留下来，得先做存档 ——
这是个独立的大活，**需要你明确要不要**。

### 9.11 "继续繁衍"的意思

"亲代留在槽位，方便用户继续繁衍" —— 两种读法：
- (a) **同一对再生一只** ⇒ 无成本无限刷，同一对生一堆；
- (b) 只是省一步（不用再拖回来），**继续繁衍指的是换亲本**。

⇒ 如果是 (a)，建议给一点代价（冷却/次数/体力），否则"独一无二"会被"批量生产"稀释。
**请确认。**

### 9.12 倒计时期间的边界情形（都需要一个明确行为）

- 倒计时中把亲代**拖出槽位** ⇒ 取消繁衍并退料？还是禁止拖出？
- 倒计时中把**另一只**拖进已占用的槽位 ⇒ 替换？（那繁衍继续还是重来？）
- 倒计时中玩家**去屠宰**这两只中的一只（如果它们还在背包里）⇒ 见 9.8
- 玩家**一直不接生**（走了十分钟）⇒ 挂起等他还是超时作废？
- 玩家**不开背包面板**，倒计时到点 ⇒ 提醒要能看到（现在 `#toast` 只有 2.6 秒，且只在 HUD 里）
- **接生只能在繁衍页做，还是任何地方都可以？**（需求没写）

### 9.13 为了能自动化测试，需要一个"快进"钩子

90 秒的流程没法在无头测试里跑（现有测试是逐帧 `loop()` 推进，且 `&still=1` 会冻结模拟）。
⇒ 需要加一个调试入口，和现有的 `&fill` / `&slay` 同一类，例如
`&breed=fast`（把 90 秒压成 0.1 秒）或暴露一个 `forceBirth()`。
**这条是"可测试性"的要求** —— 没有它这个系统只能靠手点，而手点不能回归。

### 9.14 要改的既有测试（那三条门禁是我加的，不是你的要求）

`test/integration.mjs:1345-1349` 和 `test/boot-params.mjs:77` 现在有 4 条断言在守着
"繁衍页**不许**有任何按钮/控件/规则词"。那是当初为了守"不许替用户定机制"而加的。
现在需求到了，这 4 条必须改。⇒ **我会改，并在测试里留一句"为什么改"的注释。**

---

## 10. 需要你回答的清单（按优先级）

| # | 问题 | 对应 |
|---|---|---|
| 1 | 倒计时用真实时间还是模拟时间？暂停时走不走？ | 9.1 |
| 2 | 从背包到槽位怎么操作（迷你背包 / 点选两步 / 跨页拖拽）？ | 9.2 |
| 3 | Tripo 返回的模型怎么归一化尺度（否则体型基因失效）？ | 9.3 |
| 4 | 模型没就绪 / 超时 / 失败时怎么办？倒计时与模型就绪的关系？ | 9.4 |
| 5 | 突变提示放在交配时还是接生时？说的是"位点名"还是"几个位"？ | 9.5 |
| 6 | 突变跳到哪一档（整表随机 / 相邻 / ±N）？ | 9.6 |
| 7 | 突变率 5% 是"每位独立"还是"整体一次"？接受 19.4% 完全不突变吗？ | 9.7 |
| 8 | 亲代拖进槽位时，是否真的从背包移出？能否被屠宰？ | 9.8 |
| 9 | 槽位有性别吗？同两只不能占两个槽位这条约束要吗？ | 9.9 |
| 10 | 繁衍成果要不要存档（现在刷新即丢）？ | 9.10 |
| 11 | "继续繁衍" = 同一对再生，还是换亲本？要代价吗？ | 9.11 |
| 12 | 9.12 那六种边界情形各要什么行为？ | 9.12 |
| 13 | 同意加一个 `&breed=fast` 之类的调试钩子吗？ | 9.13 |
| 14 | 上一轮的 6 个判据问题（奶龙 vs 史莱姆形 / 黏滑 vs 胶质 / 角冠 E …）还算数吗？ | 见 §4 |

> 上一轮的 §4 问题里，有几条被「模型 ≠ 主体形态」这句**部分回答**了
> （主体形态不再需要几何判据）；但**主体质感 8 档的界线**仍然要 ——
> 因为质感想在程序化球上表现出来，就必须知道自己该给什么 roughness/metalness/透明度。

---

## 11. 我还没动手的地方（避免误会）

- **`slime-ball` 改成 E 大小：还没改**，因为"E"有两种解法且后果差很多（见下面 §12）。
- 基因逻辑、繁衍流程、UI：**一行都没写**。
- 繁衍页仍是三个占位骨架。

---

## 12. 关于「slime-ball 调整成 E 大小」的一个技术分叉

E 的定义是「**长宽大 80%、高相同**」（相对 slime-ring）。而 slime-ball 是另一个模型，
它的实测比例和 slime-ring **不一样**。所以"改成 E 大小"有两种解法：

| | 目标 | 需要的缩放 | 结果 | 代价 |
|---|---|---|---|---|
| **甲：等比放大** | 让**宽**命中 E 的 2.678 | 三轴 **×1.0557** | 宽 2.678 / 高 **1.934** / 深 2.238 | 模型不变形；但**高比 E 高 5.6%、深只有 E 的 66%** |
| **乙：非等比拉伸** | 严格命中 E 的 长宽+80%、高不变 | X ×1.0557、Y ×1.0、**Z ×1.5869** | 宽 2.678 / 高 1.832 / 深 3.364 | **深被拉伸 59%**，果冻球会明显被"压扁摊开" |

（E 的目标值：宽 2.678 / 高 1.832 / 深 3.364 m；slime-ball 现状：宽 2.537 / 高 1.832 / 深 2.120 m）

⚠️ 另外，选乙还会**破坏现有代码的一个前提**：
`baseScale` 是**标量**，捕获/抛出时用 `g.scale.setScalar(a.baseScale)` 复位
（`index.html:2473`、`2513`）。非等比缩放需要改成"每轴缩放向量"，会连带改这几处。

⇒ **我倾向甲**（不变形、改动最小、且"高相同"这条其实已经满足），
但那样它在数值上就不严格等于 E。**要怎么算，请你定。**
我的建议是把 E 的定义从"相对 slime-ring"改成"相对**自身网格**的等比/非等比系数"，
那样每个网格都有自己的 E —— 但这会动到你的表，所以我只建议、不动。

---

# 第三部分：规则答复与实现状态（2026-09-23 已实现）

## 13. 用户在 §10 里的答复（照抄）

| # | 问题 | 答复 |
|---|---|---|
| 1 | 倒计时时钟 | 「用**真实时间**（现实 90 秒），玩家不需要一直开着背包，交配完成后**提醒玩家前去接生**。**暂停(Esc) 时不走时间**。」 |
| 2 | 背包 → 槽位怎么操作 | 「**右键点击**背包中的帕鲁，弹出菜单（目前只有一项：送去交配）。**交配槽位满则这一项禁止**。初始交配槽位 **6 个（3 对）**。**每个帕鲁只能进一个槽位**。」 |
| 3 | Tripo 返回模型的尺度 | 「你**调整到和基因中定义的体型一样**。」 |
| 4 | 超时/失败 | 「**你先不要考虑** Tripo 生成超时或者失败。」 |
| 5 | 突变提示 | 「说**位点名**」（并给了例子：`眼睛的有无发生了突变！！！` / `发光器官的颜色发生了突变！！！`），放在**接生时**说 |
| 6 | 突变跳到哪一档 | 「**整表随机**。」 |
| 7 | 5% 的读法 | 「"独一无二"是 **ai 生成决定的必然独一无二，突变只是个辅助**。」⇒ 每位独立 5% 保持不变 |
| 8 | 亲代是否移出背包 | 「一只帕鲁移到交配槽位后，**就不存在背包里了**，要做其它操作必须放回背包。」 |
| 9 | 性别 / 同只占两槽 | 未直接回答；已按"**无性别**"实现，并用"移出背包"天然保证了「每只只能进一个槽位」 |
| 10 | 存档 | 用户反问「刷新即丢是什么意思？」（见 §15，我解释） |
| 11 | 继续繁衍 | 「**不是一个按键，是玩家的一个选择**。两个亲本帕鲁可以放回背包，也可以留在槽位，**留在槽位也不一定要点击交配按键**。」 |
| 12 | 9.12 的边界情形 | 未逐条回答；已按"**交配进行中禁止把亲代放回**"实现（见 §14 的"我的判断"） |
| 13 | 调试钩子 | 「**加 &breed=fast 之类的调试脚手架。**」 |
| 14 | 上一轮的判据问题 | 「**"主体形态不可断言"是对的。**」⇒ 主体形态那几条作废 |

其余两条：**「slime-ball 直接改成和 slime-ring 相同大小，完全相同。我们要确保每个生物在游戏里面的大小是制式化的。」**
以及「**繁衍页现在肯定是需要改的。**」

## 14. 已实现（`index.html`）

| 需求 | 落地位置 |
|---|---|
| 体型制式化（逐轴缩放到目标盒，底面归零，烘进几何） | `BODY_BOX` / `fitGeometryToBox()`（9-0 节） |
| slime-ball = slime-ring（完全相同） | 两物种都 `body: 'A'`；实测都是 1.488 × 1.832 × 1.869 m、圆柱 r 0.93 / h 1.83 |
| 随机体型 `s` 取消（但**保留那次随机数消费**） | `buildSlimes()` 里的 `rnd();` |
| 32 位基因表（机器可读） | `GENE_DEFS`（14b2 节），带 `say`（突变提示用的说法） |
| 每位 50% 继承 + 每位 5% 突变 + 整表随机 | `inheritGenome()` |
| 突变提示 = 位点名 + 固定后缀 | `mutationLines()` |
| 基因 → 提示词 | `genomeToPrompt()` |
| 占位球（体型/主体色/质感/发光都由基因决定） | `buildOffspringBody()` |
| 6 槽 3 对 / 右键菜单 / 交配 / 90 秒真实倒计时 / 接生 | `breeding` 状态机 + `renderBag()` 第二页 |
| HUD「可以接生了」常驻提醒 | `#breedtag` + `updateHUD()` |
| `&breed=fast` / `&breed=N` | 调试参数块 |

### 我做的判断（**都标了「（我的判断）」**，改一处即可）

1. **突变的取值一定与原值不同**（整表随机可能抽回原值，那会出现"说突变了却前后一样"）。
2. `say` 里的"眼睛/耳朵/翅膀/尾巴/爪子"是口语化写法（用户例子用的是"眼睛"）。
3. `TEX_PRESET`（8 档质感 → roughness/metalness/opacity 数值）整张表是我定的。
4. 提示词文案（4 段）。
5. 现有 40 只的**初始基因**：形态=球形（用户已注明）、体型=A（实测）、质感=胶质（实测材质）、
   主体色按贴图均色落档（ring→白、ball→浅蓝）、**9 个有无位一律「无」**（与事实一致）。
6. 子代速度 = 两个亲本物种速度的平均（基因表里**没有**行动类基因，没有依据从基因推）。
7. 交配进行中**禁止**把亲代放回背包（用户没规定这种半截状态该怎么办）。
8. 「送去交配」后**不自动切页**（玩家常要连送几只），只给一句 toast。
9. 后代的名字用**主体形态**的值（如"球形"）——因为用户没给命名规则，这是唯一不新造字段的做法。
10. `&still=1`（截图冻结）也冻结倒计时（为了截图可复现）；用户只说了 Esc 暂停。

## 15. 「刷新即丢」是什么意思（回答用户的反问）

「后代要放到背包里面」这件事**实现里满足了**（`birthOffspring` 把后代 push 进 `pals`）。

我原来的意思是**另一件事**：这个 demo **没有任何持久化**。所以
- 按 **F5 刷新**、关掉标签页、或者关掉本地服务器 → 世界按种子**重新生成**；
- 你繁衍出来的后代、背包里的东西、槽位里的亲代，**全部消失**。

也就是说：**在同一个会话里后代确实在背包里；刷新之后就没了。**
要不要做存档是另一个独立的活（需要决定存什么、存在哪、什么时候写），
本轮**没有做**，也没有为它留数据结构的坑 —— 因为用户没要求，我不擅自加。

另外主动说明一处**名字上的不一致**：`pals` 这个数组名来自"背包"的中文叫法
（帕鲁 = pal），和后代的"独一无二"没有关系；`BAG_MAX = 999` 也没动。

## 16. 现在仍未定的（不阻塞实现，但定了会少改一遍）

1. **是否要存档**（见 §15）。
2. 9.12 里那几种边界情形，除了"交配中禁止放回"之外都还没定：比如
   玩家一直不接生（挂起还是作废）、接生只能在繁衍页做还是任何地方都行。
3. 主体形态里「奶龙 vs 史莱姆形」的区分 —— 用户已确认"主体形态不可断言"，
   所以**只影响提示词措辞**，不影响实现。
4. 「主体质感」8 档的界线（用户上一轮没答；现在它只在 `TEX_PRESET` 和提示词里起作用）。

---

## 17. 附属物（33~36）—— 2026-09-23 12:34 追加

### 用户原文

> 「附属物数量： A 0 、B 1 、C 2 、 D 3。
>  附属物1： A 剑 B 盾牌 C 天使光环 D 卫星式光环 E 炸药包
>  附属物2： A 剑 B 盾牌 C 天使光环 D 卫星式光环  E 炸药包
>  附属物3： A 剑 B 盾牌 C 天使光环 D 卫星式光环  E 炸药包」

### 实现（`index.html` 14b2 节）

- 4 个新基因位**追加在末尾**（33~36），下标 32~35。
  ⚠️ 位置是有意的：**不插在中间** —— 插中间会让所有已有下标平移，存量后代的基因和
  测试里的下标都会**静默错位**。这条已写进代码注释。
- `ATTACH_FORMS = ['剑','盾牌','天使光环','卫星式光环','炸药包']`，
  三行**共用同一个数组引用**（用户给的三行字面完全相同 ⇒ 共用之后改一处三行一起改），并有断言守着。
- `attachCountOf(g)` / `attachListOf(g)`：数量翻成件数、按数量取前 n 个形态位。
- **语义**：数量 = 0 ⇒ 三个形态位**不表达**（和"有无 = 无"同一套）。
  所以有效形态数 = `1 + 5 + 25 + 125 = 156`，不是 `4×5×5×5 = 500`。
- **提示词**：数量 0 ⇒ 一个字都不写；数量 ≥1 ⇒ `附属物 2 件（盾牌、卫星式光环）`。
- **突变提示**：`附属物的数量发生了突变！！！` / `附属物1的形态发生了突变！！！`。
- **占位球上不画**：剑/盾牌/光环/炸药包都要建模，属于"程序化球表达不了"的那一类
  （和 眼/耳/角冠/翅/尾/四肢/爪 一样）。这是既有决定的延续，不是漏做。

### 数字

| | 加附属物前 | 加附属物后 |
|---|---|---|
| 基因位 | 32 | **36** |
| 可突变位（去掉只有一档的「骨骼」） | 31 | **35** |
| 期望突变 / 只（每位 5%） | 1.55 | **1.75** |
| 组合空间（直接相乘） | 5 × 10²⁰ | **2.5 × 10²³** |
| 组合空间（按"不表达"折叠） | 1.2 × 10¹⁸ | **1.9 × 10²⁰** |

### ⚠️ 一个需要你定的问题（不阻塞，已按原文实现）

**附属物要不要也有颜色？** 你的另外 9 组部件都是「有无 + **颜色** + 形态」三件套，
这四组是「数量 + 形态」，**没有颜色**。两种可能：
- 本来就不打算给附属物上色 ⇒ 现状即最终形态；
- 漏写了 ⇒ 告诉我，我按同样的 13 档补三行「附属物1/2/3·颜色」。

我按当前原文实现（**没有替你补**）。另外顺带一句：如果以后要加附属物的颜色位，
正确做法仍是**追加在末尾**，而不是插在 34~36 附近 —— 理由同上（下标平移）。

---

## 18. 融合：把"太空舱交配内容"并回主线（2026-09-23）

用户指令：「融合 `C:/Users/26335/WorkBuddy/2026-09-23-13-12-23` 的交配内容」。
追问后用户定了两件事：**全盘采用单舱**、**我自己完整验一遍**（不沿用支线"未验证"的状态）。

### 支线是什么 / 怎么定的融合方向

先摸了底，结论决定了做法：那条支线**不是另起一套**，而是**从本主线的一张快照分出去的**
（`index.html` 里 `GENE_DEFS` / `BODY_BOX` / `fitGeometryToBox` / `measureCylinder` /
`spawnAnimal` / `HOP_TRACK` / `drawMinimap` / `breeding` 全在），
唯一的差别是：**它没有附属物那 4 位（33~36）**，而多了一整套太空舱。

⇒ 所以融合方向定为：**以它那棵树为基线，把主线的增量补回去**，
而不是把这套舱硬塞进主线（后者要重做 42 个 hunk + 迁移整套测试，代价大得多且更容易出错）。

### 并进来的东西

- `chamber-system.mjs`（状态机 + 宿主回调）/ `chamber-view.mjs`（世界里那对亲代槽 + 观察窗）
  / `chamber-performance.mjs`（满屏演出与 QTE）/ `chamber-stage.mjs` / `chamber.css`
- `fusion-v1/`：`wave-motion` / `recombination-motion` / `combine-session` / `jelly-deform` /
  `game-actor` / `surface-contact` / `splash-motion` / `slime-splash` / `combine-audio`
- `assets/`：背景乐、激光音效、演出音轨（打包时内联成 base64 数据块）
- `test/chamber.mjs` / `test/standalone.mjs` / `test/run.mjs`
- `docs/` 6 篇 + `ARCHITECTURE.md`

**架构上是干净的**：舱只做表现层，遗传与出生仍然调主线的 `startMate(0)` / `birthOffspring(0,'chamber')`
—— 设计文档里也明确写了"不自行实现另一套遗传算法"。这一点让融合变得可行。

### 基因表受的影响（本文件关心的部分）

- **36 位不变**（支线缺的 33~36 补回了）。
- `BREED_PAIRS` **3 → 1**（舱只有左右两个槽位）⇒ `BREED_SLOTS` 2。
  ⇒ §14 那条"6 槽 3 对（用户指定）"**被这一轮的用户决定取代**：现在是一对。
- `#palMenu`（右键「送去交配」）与背包第二页整套删除 ⇒ §14 里那两条交互描述作废。
- 后代的 `g.userData.speed` 现在会被写上（演出要用），并且 `birthOffspring(p, destination)`
  多了一个目的地参数（`'chamber'` = 先出生到舱里、等玩家领取；`'bag'` = 直接进背包）。

### 修掉的两个缺陷

1. **`replay resets presses`**（支线的真 bug）：`createCombineSession().start()` 在
   `state === 'running'` 时**早退**，而"重播"是直接调 `start()` 的 ⇒ 在 QTE 阶段点重播
   不会重置 presses/time/surgeRound，但演出已被摆回 0 位。
   ⇒ 修法：重播前先 `session.reset()`。**只在演出前半段出现**，所以很容易漏。
2. **`next generation reuses original inheritance`**（融合引入的）：断言把长度写死成 `32`。
   ⇒ 改成跟 `GENE_COUNT` 走。

### 验收（现跑，不是转述）

- `node test/run.mjs` 全绿：integration **355**（含补回的 15 条附属物断言）/ input **149** /
  boot-params **41+6+5+18+1** / chamber **47** / scene-cost **28** / 单文件版构建一致。
- **新写 `_probe_chamber.mjs`（真浏览器整场演出）→ 28/28**：
  `密封 → 可以结合 → 按 E → QTE 10 次 → 强化 0→1→2→3→4 → cinematic
  entry→drawing→weaving→coiling→sphere→separating→birth→result → complete → 领取 → 回等待`，
  约 16 秒、0 控制台 error。
- `_probe_hud.mjs` **84/84**（第五轮从"繁衍页"迁到"舱 HUD"）；`_probe_dnd.mjs` **44/44**。

### 「卡在强化结合 0 / 4」的结论

**复现不出来。** 强化阶段在真浏览器里确实按 0→1→2→3→4 推进、演出走完 8 个阶段。
⇒ 那次卡住更可能是**环境性**的：演出推进挂在
`running && !paused && !document.hidden`（`chamber-performance.mjs` 的 update），
标签页不可见 / 被暂停 / 开着背包 / `&still=1` 都会让它停在原地。**不是强化算法坏了。**

### 仍需你定的（承接 §17）

- 附属物要不要颜色位 —— 支线也没管这件事，仍然**按你的原文实现**。
